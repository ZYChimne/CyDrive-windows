<Window x:Class="CyDrive.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CyDrive"
        mc:Ignorable="d"
        Title="Settings" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Button Content="返回" Grid.Row="0" BorderThickness="0" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Center" Background="White" Margin="60, 0, 0, 0"/>
        <Label Grid.Row="1" Content="CyDrive" FontSize="180" Foreground="SkyBlue" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>
                <Button Content="设置" Grid.Row="0" BorderThickness="0" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White"/>
                <Button Content="关于" Grid.Row="1" BorderThickness="0" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Background="White"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
